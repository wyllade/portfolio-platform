// Defines a reusable React functional component for the application header.
function Header() {
  return (
    // Serves as the main header section of the application
    <header>

      <h1>Portfolio Platform</h1>

    </header>
  );
}

// Makes the Header component available for use in other files
// The Header component cannot be imported, causing errors where it is used.
export default Header;